import React from 'react';
// import ReactDOM from 'react-dom';
import {createRoot} from 'react-dom/client';

const el = <div>Hello every buddy</div>
const root = createRoot(document.getElementById('root'))
root.render(el)
console.log(123123)